<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.strap">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Strap">
        <activity
            android:name=".viewmodel.activity.TodayRoutineActivity"
            android:name=".viewmodel.activity.NowExercisingActivity"
            android:name=".viewmodel.activity.StartExercise"
            android:name=".viewmodel.activity.AddWorkoutActivity"
            android:name=".viewmodel.activity.AddRoutineActivity"
            android:name=".viewmodel.activity.MainActivity"
            android:name=".viewmodel.activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".viewmodel.activity.MainActivity" />
        <activity android:name=".viewmodel.activity.SelectFitnessActivity"/>
    </application>

</manifest>